# Never commit a real key here — this file is a template only.
ANTHROPIC_API_KEY=

# Model routing (docs/architecture-plan.md SS D/H) — override per environment
# without touching any calling code.
VISION_MODEL=claude-sonnet-5
WORKOUT_PARSE_MODEL=claude-haiku-4-5
COACH_MODEL=claude-sonnet-5

VISION_MAX_TOKENS=2048
WORKOUT_MAX_TOKENS=1024
COACH_MAX_TOKENS=2048

REQUEST_LOG_DB_PATH=ai_service_requests.db

HOST=0.0.0.0
PORT=8000
