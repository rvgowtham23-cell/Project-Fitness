# Base URL of the backend REST API (NestJS), including the /api/v1 version prefix.
# See docs/architecture-plan.md §G for the endpoint contract this client is built against.
NEXT_PUBLIC_API_BASE_URL=http://localhost:3001/api/v1
